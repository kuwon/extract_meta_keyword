제공된 파일에서 증권 업무 특성을 나타내는 핵심 키워드를 추출한다.
제시되는 INPUT DATA 파일에서 각각의 row에 대한 결과 값을 생성하여 새로운 파일로 만든다

[INPUT DATA]
- 첨부한 merged.csv 파일
- 테이블과 그에 대한 설명 값, 그리고 보유하고 있는 컬럼 리스트로 되어 있음 
- 컬럼은 테이블영문명, 테이블국문명, 설명 키워드, alt_names로 되어 있음
- "설명 키워드" 컬럼은 값이 있을수도 있고 없을수도 있음
- alt_names 컬럼은 각 테이블이 보유하고 있는 컬럼 리스트이다

이에 대한 결과 양식과 활용할 가이드라인은 다음과 같아.
[OUTPUT FORMAT]
키워드는 JSON 형식으로 출력해야 한다.
- TABLE: INPUT DATA의 "테이블영문명"을 그대로 사용
- KEYWORDS: INPUT DATA의 alt_names에서 주요하다고 판단되는 항목 5개를 추출. 혹시 INPUT DATA의 "설명 키워드" 가 빈값이 아니라면 여기에서 2개를 추출하고 나머지 3개는 alt_names에서 추출. 테이블명에서도 아래 가이드라인을 통해서 추출할 수 있음

[KEYWORD GUIDELINES]
- 테이블명을 그대로 키워드로 추출하지 않음 (예: '일별발행어음매수집계'가 테이블명이라면 '일별발행어음매수집계'는 키워드가 될 수 없음)
- 테이블명이 ['집계', '내역', '코드', '기본'] 으로 끝나면 그걸 제외한 값은 키워드 후보가 될수 있음 (예: '일별발행어음매수집계'가 테이블명이라면 '발행어음매수'는 키워드 후보)
- 테이블명에 '_실시간0' 또는 '_실시간1'로 끝난다면 그걸 제외한걸 감안해주고, '실시간' 키워드도 후보로 넣을수 있음
- 태이블명이 'V_TRAD_', 'V_RMCP_', 'V_PRDT_', 'V_MGMT_'로 시작하면 그걸 제외한걸 테이블 이름이라고 생각해주고, 이것들은 INPUT DATA의 설명 정보를 좀 더 주의깊게 보고 4:1 비율로 뽑아줘. 여기에 있는 원천 테이블 정보도 고려해줘.
- 'DW', 'ETL'이 포함된 단어는 키워드가 될수 없다. 
- 'XX코드', 'XX코드관리'는 키워드가 될수 없다
- '조작XX'으로 시작하는 operation 관련한 항목들도 키워드가 될 수 없다.
- '기준일자', '등록자직원번호'나 '변경자직원번호'도 키워드가 될 수 없다.
- 복합 단어 우선. 단일 개념이라도 업종 업무 특성을 분명히 나타내면 허용한다
- 하나의 복합업무에 유사한 의미들은 한번만 추출한다 (예: 랩모델상품전환신청,랩모델전환신청관리 -> 랩모델전환신청)
- 공백은 포함하지 않는다
- 키워드에 'XX코드'가 식별되면 반드시 '코드'는 '정보'로 변경하여 출력 (예: 계좌상품코드 -> 계좌상품정보)
- 키워드 갯수는 최대한 5개를 맞추도록 함.